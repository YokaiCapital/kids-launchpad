import {ArrowRight,ArrowLeft} from '@phosphor-icons/react';
export function WinnerPrelaunch({coin,go}){
 if(!coin)return <section><h1>Proposal unavailable</h1><button onClick={()=>go('Vote')}>Back to voting</button></section>;
 return <section className="winner-prelaunch"><div className="winner-main"><button className="text-button" onClick={()=>go('Vote')}><ArrowLeft size={18}/> Back to ballot</button><p className="eyebrow">ROUND 009 · OUTCOME PREVIEW</p><h1>{coin.name}</h1><p className="subtitle">{coin.parents.join(' × ')} → ${coin.ticker}</p><p>{coin.description}</p><div className="winner-progress" aria-label="Launch readiness"><span>1 · Proposal</span><span>2 · Holder vote</span><strong>3 · Launch checks</strong><span>4 · Prelaunch</span></div><h2>Before commitments open</h2><div className="winner-checks"><div><strong>Parent tokens</strong><span>Verify both mint addresses</span></div><div><strong>Allocation terms</strong><span>Publish token supply, cap and refunds</span></div><div><strong>Launch setup</strong><span>Verify escrow and opening schedule</span></div></div><p className="small muted">Sample election outcome. This proposal has not launched; funding and claims are not open.</p></div><aside className="commit-panel winner-card"><div className="coin-banner"><span>{coin.parents.join(' × ')}</span><strong>{coin.name}</strong></div><div className="winner-identity"><img src={coin.art} alt=""/><div><h2>{coin.name}</h2><span>${coin.ticker}</span></div><span className="status-tag">Checks pending</span></div><h3>Prelaunch is next.</h3><p className="muted">Commitments open after the launch checks and terms are published.</p><button className="primary" disabled>Commitments not open</button><button className="text-button" onClick={()=>go('Shart')}>Explore Shartcoin’s prelaunch <ArrowRight size={18}/></button></aside></section>;
}
